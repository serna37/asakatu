#include <bits/stdc++.h>
using namespace std;
const double PI = acos(-1);
int main() {
    int R, D;
    cin >> R >> D;
    cout << fixed << setprecision(20);
    cout << R * R * PI * 2 * D * PI << endl;
}
